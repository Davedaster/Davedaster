name: Agent migrate notification hotfixes

on:
  push:
    branches:
      - agent/migrate-notification-hotfixes

permissions:
  contents: write

jobs:
  migrate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out migration branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: 20.19.0

      - name: Apply existing notification hotfix output
        run: node scripts/agent-migrate-notification-hotfixes.mjs

      - name: Check patch formatting
        run: git diff --check

      - name: Commit migrated source
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Migrate notification hotfixes into source"
          git push origin HEAD:${{ github.ref_name }}
